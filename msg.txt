feat: production hardening — sidecar proxy/captcha, error handling, observability

Python sidecar:
- ScrapeRequest accepts proxy + captchaToken fields
- Orchestrator passes proxy/captcha_token through to backends
- Playwright backend applies proxy config at launch, injects captcha tokens

Error handling:
- Global Fastify setErrorHandler catches all uncaught errors
- Sanitizes internal errors (API keys, tokens, stack traces) before sending

Observability:
- Request correlation IDs (crypto.randomUUID) via genReqId
- X-Request-Id header on every response
- Correlation ID in request logs for tracing
- requestId in error responses for support debugging

Input validation:
- Zod schemas for web.ts (scrapeSchema), billing.ts (createOrder/verifyPayment/createSub), session.ts (navigate)

Sentry:
- Conditional import — works if @sentry/node installed, skips gracefully if not
- SENTRY_DSN env var added to .env.example
- 500 errors reported to Sentry with request context

Dashboard fix (PostCSS/Tailwind):
- Resolved system-level Tailwind v4 PostCSS config leak

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
